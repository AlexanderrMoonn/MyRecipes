{
  "name": "family-recipes",
  "version": "2.0.0",
  "description": "Static family recipe site for Cloudflare Pages. Recipes live as files in the repo; the build regenerates the manifest; an optional Pages Function lets you add recipes from the site.",
  "scripts": {
    "build": "node build.js"
  },
  "engines": {
    "node": ">=18"
  }
}
